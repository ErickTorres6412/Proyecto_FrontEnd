import { Package } from 'lucide-react';

const modules = [
  {
    title: 'Departamentos',
    icon: Package,
    subItems: [
      { label: 'Departamentos', link: '/departamentos' }
    ]
  }
];

export default modules;
