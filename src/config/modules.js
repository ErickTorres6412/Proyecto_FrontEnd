import { Package } from 'lucide-react';

const modules = [
  {
    title: 'Examen 2',
    icon: Package,
    subItems: [
      { label: 'Info Examen', link: '/visualizar-condominios' }
    ]
  }
];

export default modules;
